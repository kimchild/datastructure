package java8;

import java.io.File;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.util.Arrays;
import java.util.Collections;
import java.util.List;
import java.util.Objects;

/**
 * OptionalTest
 *
 * @author ckkim on 2018-04-25
 */
public class OptionalTest {

    public static void main(String[] args) {
//        Path readPath =
//                Paths.get(FilenameUtils.normalizeNoEndSeparator(rootFilePath)
//                        , active.getDataSourceId()
//                        , tda.getPathDate()
//                        , tda.getPathTime());
//        List<File> fileList = (readPath.toFile().exists()) ?
//                Arrays.asList(Objects.requireNonNull(readPath.toFile().listFiles())) : Collections.emptyList();

        Runnable runnable = new Runnable() {
            @Override
            public void run() {}
        };
    }
}
